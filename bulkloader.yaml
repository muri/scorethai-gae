# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Log
  connector: csv
  connector_options:
    encoding: utf-8
    print_export_header_row: true
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: author_name
      external_name: author_name

    - property: categories
      external_name: categories
      # Type: String Stats: 180 properties of this type in this kind.

    - property: date
      external_name: date
      # Type: Date/Time Stats: 242 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: description
      external_name: description
      # Type: Text Stats: 242 properties of this type in this kind.
      import_transform: db.Text

    - property: info
      external_name: info
      # Type: Category Stats: 242 properties of this type in this kind.
      import_transform: db.Category

    - property: link
      external_name: link
      # Type: String Stats: 242 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 242 properties of this type in this kind.


- kind: ScoreData
  connector: csv
  connector_options:
    encoding: utf-8
    print_export_header_row: true
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: content
      external_name: content
      # Type: Text Stats: 155 properties of this type in this kind.
      import_transform: db.Text

    - property: date
      external_name: date
      # Type: Date/Time Stats: 155 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: summary
      external_name: summary
      # Type: String Stats: 155 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 155 properties of this type in this kind.

    - property: user
      external_name: user
      # Type: User Stats: 155 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: version
      external_name: version
      # Type: Integer Stats: 155 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Score
  connector: csv
  connector_options:
    encoding: utf-8
    print_export_header_row: true
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: categories
      external_name: categories
      # Type: String Stats: 78 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 78 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: creater
      external_name: creater
      # Type: User Stats: 78 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: deleted
      external_name: deleted
      # Type: Boolean Stats: 78 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: lastmodified
      external_name: lastmodified
      # Type: Date/Time Stats: 78 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastmodifier
      external_name: lastmodifier
      # Type: User Stats: 78 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: summary
      external_name: summary
      # Type: String Stats: 78 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 78 properties of this type in this kind.


- kind: UserInfo
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    encoding: utf-8
    print_export_header_row: true
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: can_addnew
      external_name: can_addnew
      # Type: Boolean Stats: 6 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: can_delete_other
      external_name: can_delete_other
      # Type: Boolean Stats: 6 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: can_modify_other
      external_name: can_modify_other
      # Type: Boolean Stats: 6 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: isoperator
      external_name: isoperator
      # Type: Boolean Stats: 6 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: lastlogin
      external_name: lastlogin
      # Type: Date/Time Stats: 6 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: user
      external_name: user
      # Type: User Stats: 6 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

